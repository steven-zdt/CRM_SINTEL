# Python / Django
__pycache__/
*.py[cod]
*$py.class
*.sqlite3
*.log
.env

# Entornos y caches locales
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# EKG dry-run output (generated, see tools/ekg/PILOT_REPORT.md)
tools/ekg/out/
.cache/

# E2E (Playwright) -- instalado via contenedor efimero, no en el repo (F32.5)
tests/e2e/node_modules/
tests/e2e/test-results/

# Artefactos generados localmente
staticfiles/
media/
scratch/
brain/
ARTIFACTS/

# Celery Beat -- shelve db local del scheduler (AI-VECTOR-11), nunca en git.
celerybeat-schedule

# Salidas temporales de pruebas y depuracion
pytest_*.txt
pytest_*.log
test_*.txt
test_*.log
debug_*.txt
step*_prompt.txt
user_prompt.txt
temp_*.py

# Configuracion local de asistentes/IDEs
.claude/settings.local.json

# Backups de base de datos -- pueden contener datos reales de tenants
# (PII, facturas, etc.), nunca deben llegar a git.
backups/
