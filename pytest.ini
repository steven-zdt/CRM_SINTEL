[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py *_test.py tests.py
norecursedirs = venv .venv .git nul brain ARTIFACTS .antigravity .claude .cursor .pytest_cache documentacion scratch scripts
django_find_project = true
markers =
    e2e: tests that exercise a live_server via Playwright (not run in the default suite)
