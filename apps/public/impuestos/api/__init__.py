"""
API REST para la app impuestos (catálogo DIAN).

Esta app está en SHARED_APPS, por lo que el catálogo es compartido
y está disponible para todos los tenants.
⚠️ IMPORTANTE: Los ViewSets son ReadOnly (solo lectura) porque es un catálogo.
"""
