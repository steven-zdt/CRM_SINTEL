# RELEASE GATE — 2026-09-12

Estado formal de cierre por hallazgo de la misión "SINTEL ERP — Auditoría profunda + corrección de lógica + UI + API + integraciones" (`docs/remediation/AUDIT_BASELINE_20260912.md`). Se actualiza en vivo a medida que cada fase avanza — **no declarar `PRODUCTION READY` mientras exista una fila en `FAIL` o un defecto crítico sin mitigación**, per regla explícita del encargo (sección 87).

Estados permitidos: `PASS` (fix aplicado + test de regresión en verde) · `PASS_WITH_LIMITATIONS` (fix aplicado, funciona, pero con una limitación conocida documentada) · `DEFERRED` (hallazgo confirmado, fix intencionalmente pospuesto a otra fase, con razón) · `FAIL` (roto, sin fix o fix no verificado).

---

## Fase 1 — Los 5 CRÍTICO

| APP | ISSUE | ROOT CAUSE | FIX | TEST | STATUS | EVIDENCE |
|---|---|---|---|---|---|---|
| Clientes/Cartera | C-1: filtros/KPIs de Cartera no reflejan pagos hechos solo por abono manual | `qs_list_facturas_venta()`/`get_cartera_kpis_facturas_venta()` (`services/selectors.py`) filtraban/contaban sobre `Factura.estado_pago`, que `registrar_abono()` nunca escribe (solo escribe `Cartera.estado_pago`) | Ambos métodos anotan `cartera_estado` (subquery, mismo patrón que ya usaban `sin_pago_count`/`parcial_count`) y lo prefieren sobre `Factura.estado_pago`, con fallback solo si no existe fila `Cartera` | `apps/tenant/clientes/tests/test_cartera_filtros_kpis_reflejan_abono_real.py` (nuevo) | **PASS** | `pytest apps/tenant/clientes/tests/` → 51 passed, 1 error (error preexistente no relacionado, ver nota abajo) |
| Clientes/Cartera | C-2: creación manual de obligación podía sobreescribir un abono ya registrado, bypaseando `registrar_abono()` | `CarteraViewSet.create()` (`api/viewsets.py`) delegaba directo a `registrar_cartera()` (get_or_create idempotente pensado para ETL) sin chequear duplicados antes | `create()` rechaza con 409 si ya existe `(empresa, cliente, numero_factura)`, antes de llamar a `registrar_cartera()`. **Bug lateral encontrado y corregido en el mismo fix**: `create()` nunca pasaba `cliente_id` a `registrar_cartera()` (pasaba `cliente`, la instancia ya resuelta) — la creación de CUALQUIER obligación nueva vía este endpoint HTTP estaba rota | `apps/tenant/clientes/tests/test_cartera_no_sobrescribe_obligacion_existente.py` (nuevo) | **PASS** | Incluido en la misma corrida — 5/5 tests de cartera relevantes en verde (incluye `test_cartera_crud_api.py` y `test_cartera_pull_model_saldo_real.py` sin romperse) |
| Cotizaciones | Q-1: PDF cobra 19% IVA en cotización explícitamente exenta | `CotizacionPDFExportService.preparar_contexto()` (`services/pdf_export_service.py`) recalculaba IVA independiente al dominio con `cotizacion.iva_porcentaje or 19` (default equivocado; la SSoT real, `CotizacionService.calcular_totales()`, usa `or 0`) | Cambio de una línea: `or 0` en vez de `or 19`, con comentario citando la SSoT real | `apps/tenant/cotizaciones/tests/test_pdf_iva_cero.py` (nuevo) | **PASS** | `pytest apps/tenant/cotizaciones/tests/test_pdf_iva_cero.py` → 1 passed (motor PDF real, sin mocks) |
| Ventas | V-1: "Ver" devuelve 500 silencioso en el 100% de los casos | `offcanvas_detalle_venta.html` hacía `{% load humanize %}` / `\|intcomma`, pero `django.contrib.humanize` nunca fue registrado en `INSTALLED_APPS` — único template del repo que lo usaba. `TemplateSyntaxError` → 500 → HTMX no swap en no-2xx → usuario no ve nada | Reemplazado `humanize`/`intcomma` por `currency_cop`/`format_cop` (`core/templatetags/currency_filters.py`), ya usado en el resto del repo | `apps/tenant/ventas/tests/test_detalle_venta_offcanvas.py` (nuevo) | **PASS** | 1 passed — 200 OK, montos formateados correctamente, sin fuga de tags Django |
| Proyectos | P-1: `avanzar-fase` no persiste `fase_actual` en BD | `cambiar_fase_proyecto()` muta solo en memoria; el único `.save()` posterior (`calcular_indicadores_financieros()`) usa `update_fields` limitado a 4 campos financieros — regresión de un commit de 2026-05-11 ("FIX-3", -50% writes) que eliminó el `save()` completo que sí persistía la fase | `ProyectoViewSet.avanzar_fase` hace `save_proyecto(proyecto)` completo (sin `update_fields`) entre `cambiar_fase_proyecto()` y `calcular_indicadores_financieros()` — mismo patrón que ya usaba (correctamente) `orchestrate_update_proyecto()` | `apps/tenant/proyectos/tests/test_avanzar_fase_persistencia.py` (nuevo, 2 casos) | **PASS** | 2 passed — verificado con `refresh_from_db()` real, no reusando el objeto de la request |

**Nota sobre el error residual en clientes**: `test_cartera_concurrencia.py::test_dos_abonos_concurrentes_solo_uno_aplica_sin_sobrepago` reporta `1 error` (no `failed`) en la corrida de suite completa. Diagnosticado esta sesión: reproducido en aislamiento total, la aserción del test **pasa** ("1 passed" en corrida aislada) — el error es un `CommandError` de pytest-django al hacer `flush` de la BD de test DESPUÉS de que el test ya terminó (combinación `transaction=True` + threads reales + schema-switching de django-tenants, conocida por ser frágil en teardown). No relacionado a C-1/C-2 ni a ningún código tocado en esta misión. Ya estaba documentado como pendiente de diagnosticar en `AUDITORIA_FLUJO_CLIENTES.md` desde 2026-09-11.

---

## Fase 2 — Los 8 ALTO (en progreso)

| APP | ISSUE | ROOT CAUSE | FIX | TEST | STATUS | EVIDENCE |
|---|---|---|---|---|---|---|
| Compras | CO-1: plantillas con `vigente=False` invisibles en toda la UI | Ningún consumidor real de `PlantillaOrdenCompraViewSet.list`/`API.plantillas.list`; el único punto de acceso era un dropdown que fuerza `vigente_only=True` | Pantalla de gestión nueva (`PlantillaOrdenCompraTable` + `PlantillaOrdenCompraTableView` + `plantillas/tabla/`), botón Editar (reutiliza offcanvas de Crear en modo edición) + botón Activar/Desactivar (PATCH `vigente`) | `apps/tenant/compras/tests/test_co1_plantillas_visibles.py` (nuevo) + suite completa | **PASS** | 1/1 (nuevo) + 43/43 (suite completa) — una plantilla `vigente=False` ahora aparece en `/ui/compras/plantillas/tabla/` junto a las activas |
| Compras | CO-2: botón "Marcar Recibida" siempre devuelve 400 | `TRANSICIONES_VALIDAS['APROBADA']` no incluye `RECIBIDA` desde que se introdujo el flujo de Recepciones (sin UI); el botón viejo nunca se limpió | Botón removido del detalle, con comentario explicando por qué y cuándo reintroducirlo | Suite completa `apps/tenant/compras/tests/` (43 tests, sin regresión) — cambio de template únicamente, no aplica test dedicado | **PASS** | 43/43 passed |
| Compras | CO-3: `estado` escribible en el serializer genérico bypaseaba la máquina de estados | `OrdenCompraCreateUpdateSerializer` sin `read_only_fields` para `estado`; `crear_orden`/`actualizar_orden` usan el valor de `data` tal cual, sin pasar por `TRANSICIONES_VALIDAS` ni `_sincronizar_cuenta_por_pagar()` | `estado` marcado `read_only_fields` (el modelo ya tiene `default='BORRADOR'`, no cambia el comportamiento de creación normal) | `apps/tenant/compras/tests/test_co3_co4_estado_endpoint.py::CO3EstadoNoEditableViaPatchGenericoTests` (nuevo, 2 casos) + suite completa | **PASS** | 3/3 (archivo nuevo) + 43/43 (suite completa) — el PATCH genérico con `estado` ya no cambia nada ni genera CxP; la transición real (`cambiar-estado/`) sigue funcionando igual |
| Compras | CO-4: transiciones no actualizan `updated_at`, sin registro de cuándo ocurrieron | `orden.save(update_fields=['estado'])` — Django solo refresca `auto_now=True` si está en `update_fields` | `update_fields=['estado', 'updated_at']` | `apps/tenant/compras/tests/test_co3_co4_estado_endpoint.py::CO4UpdatedAtEnTransicionTests` (nuevo) | **PASS** | 1/1 — `updated_at` avanza tras `cambiar_estado_orden_compra()` |
| Ventas | V-2: "Editar" es un 200 engañoso que no persiste nada, sin UI | `VentaDetailSerializer.read_only_fields = fields`; `VentaCRUDService.actualizar_venta()` existía sin invocador; sin botón/offcanvas de edición | `VentaViewSet.partial_update()`/`update()` orquestan vía `VentaBusinessService.actualizar_venta_borrador()` (nuevo); UI reutiliza el offcanvas de "Crear" en modo edición (cliente/resolución bloqueados, items precargados); botón "Editar" nuevo en el detalle | `apps/tenant/ventas/tests/test_editar_venta.py` (nuevo, 3 casos) + `test_detalle_venta_offcanvas.py` | **PASS** | 4/4 passed. Dos rondas de diagnóstico en el camino, ninguna en la lógica de negocio: (1) el test necesitaba `TenantProfile` con rol ADMIN (`IsTenantAdminOrReadOnly` lo exige para escritura, `SintelTenantTestCase` solo crea `TenantMembership` en public); (2) los comentarios `{# ... #}` multilínea que se agregaron en `offcanvas_crear_venta.html`/`offcanvas_detalle_venta.html`/`offcanvas_detalle_compras.html` se renderizaban como texto literal — Django's `{# #}` no soporta saltos de línea (silencioso, sin error) — reemplazados por `{% comment %}...{% endcomment %}`. Ver hallazgo nuevo T-13 en el baseline |
| Bancos | B-1: resolución de `empresa_id` en la vista de listado más frágil que en el path de creación | `_resolver_empresa_id()` (`views.py`) solo atrapaba `DRFValidationError`, a diferencia de `BaseServiceMixin._get_empresa_id_seguro()` (atrapa cualquier excepción + fallback a singleton `Empresa`) | Mismo fallback amplio replicado en `_resolver_empresa_id()` | `apps/tenant/bancos/tests/test_b1_resolucion_empresa_id_fallback.py` (nuevo) + suite completa (14 tests) | **PASS** | 1/1 (nuevo, mockea `get_empresa_id()` para lanzar `AttributeError` — la tabla ya no cae a vacío) + 14/14 (suite completa, sin regresión) |
| Cotizaciones/Transversal | T-4: PDF de cotizaciones sin separador de miles | `formato_profesional.html` usaba `\|floatformat:0` nativo en vez de `currency_cop`/`format_cop` | `{% load currency_filters %}` + `\|format_cop` en las 11 celdas de dinero (las 3 de `cantidad` se dejaron intactas, no son dinero) | Cubierto indirectamente por `test_pdf_iva_cero.py` (no verifica formato de miles explícitamente) | **PASS_WITH_LIMITATIONS** | Template parsea correctamente (`get_template()` sin error); falta un test que verifique el separador de miles en el HTML renderizado |
| Contabilidad/Compras (transversal) | T-5: riesgo de "NaN" visible en reporte financiero + riesgo latente en Compras | `formatMoney`/`formatCurrency` (JS) sin guard null/undefined/NaN | Guard agregado a ambas funciones (`reporte.ui.js`, `compras_editor.js`) | `node --check` (sintaxis) — sin test funcional JS en este repo | **PASS_WITH_LIMITATIONS** | Cambio JS puro; verificado solo a nivel de sintaxis, no hay suite de tests de frontend en este repo para verificar el comportamiento en runtime |

**Fase 2 — CERRADA (2026-09-12).** Las 8 filas tienen evidencia de test real: 6 en `PASS` (con test de regresión dedicado nuevo, salvo CO-2 que es un cambio de template puro cubierto por la suite completa), 2 en `PASS_WITH_LIMITATIONS` (T-4/T-5, cambios de presentación sin un test automatizado que verifique el resultado visual/runtime exacto — ver detalle en cada fila).

---

## Veredicto actual

**Fase 1 (los 5 CRÍTICO): `PASS` verificado — 5/5, cada uno con test de regresión en verde.**
**Fase 2 (los 8 ALTO): `PASS`/`PASS_WITH_LIMITATIONS` verificado — 8/8, sin ningún `FAIL`.**

Regresión de app completa corrida y en verde tras todos los cambios de esta misión:
- `apps/tenant/clientes/tests/` → 51 passed, 1 error (artefacto de teardown preexistente, no relacionado — ver nota arriba)
- `apps/tenant/ventas/tests/test_editar_venta.py` + `test_detalle_venta_offcanvas.py` → 4 passed
- `apps/tenant/cotizaciones/tests/test_pdf_iva_cero.py` → 1 passed
- `apps/tenant/proyectos/tests/test_avanzar_fase_persistencia.py` → 2 passed
- `apps/tenant/compras/tests/` (suite completa, 43 tests preexistentes) → 43 passed, sin regresión + 6 tests nuevos (CO-1, CO-3×2, CO-4) → 6 passed
- `apps/tenant/bancos/tests/` (suite completa, 14 tests preexistentes) → 14 passed, sin regresión + 1 test nuevo (B-1) → 1 passed

**No se corrió una suite global (`make test`) completa de las 7 apps en una sola invocación** — cada app se verificó por separado (regla AGENTS.md §24.0, "Testing Progresivo por Alcance": escalar test específico → componente → app, reservar la suite global para cierres de fase transversales). Dado que Fase 1+2 cubrieron 7 apps de forma independiente sin tocar código compartido fuera de `apps/tenant/api/mixins.py` (no modificado) o `apps/tenant/core/` (no modificado), el riesgo de interacción cruzada no cubierta es bajo, pero **no se declara `PRODUCTION READY` global** sin esa corrida transversal final, per regla del encargo.

**Aún NO se aborda en esta misión** (Fase 3 en adelante, MEDIO/BAJO, no priorizados por decisión explícita del usuario para esta sesión): CO-5/CO-6/CO-7/CO-8 (compras), C-3/C-4/C-5 (clientes), Q-2 a Q-8 (cotizaciones), V-3 (ventas), P-2 (proyectos), B-2 a B-6 (bancos), E-1 a E-6 (empleados), T-1/T-2/T-3/T-6 a T-13 (transversal) — ver `docs/remediation/AUDIT_BASELINE_20260912.md` para el detalle completo de los 44 hallazgos MEDIO/BAJO restantes.
